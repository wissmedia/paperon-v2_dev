<!DOCTYPE html>
<html lang="en">
<!-- HEAD PARTIALS -->
<%- include('../partials/head.ejs') %>

  <body>
    <!-- NAV PARTIALS -->
    <%- include('../partials/nav.ejs') %>
      <div class="container">
        <div class="row-content">
          <form action="/viewer" method="POST">
            <% if(questions.length> 0){ %>
              <% questions.forEach(question=> { %>
                <div class="content fadeInKiri">
                  <div class="line"></div>
                  <%- helper.typeRender(question.tipe, question.useWajib, question.useEtc, question.useOpsi,
                    question.body, question.opsiy, question.opsix) %>
                </div>
                <% }) %>
                  <center><button type="submit" class="tombol-md tmb-utama bunder fadeInBawah">Simpan</button></center>
                  <% }else{ %>
                    <div class="content fadeInkiri">
                      <div class="line"></div>
                      <div class="bungkus-content edit">
                        <h3>Tidak Ada Data</h3>
                      </div>
                    </div>
                    <% } %>
          </form>
        </div>
      </div>

      <!-- MODAL AUTH -->
      <%- include('../partials/modal-auth.ejs') %>
        <!-- FOOTER PARTIALS -->
        <%- include('../partials/footer.ejs') %>
          <!-- SCRIPT PARTIALS -->
          <%- include('../partials/script.ejs') %>
            <script src="/js/quesioner-view.js"></script>
  </body>

</html>